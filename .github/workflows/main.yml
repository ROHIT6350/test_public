name: Simple GitHub Action

on: [push]

jobs:
  build:
    runs-on: slef-hosted

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Say Hello
      run: echo "Hello, this is a simple GitHub Action running!"
